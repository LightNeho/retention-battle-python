# RETENTION BATTLE — הוראות התקנה ופריסה

## שלב 1: יצירת הפרויקט

1. פתח **Google Sheets** חדש וריק. זה יהיה ה-Database.
2. בתפריט: **Extensions → Apps Script**.
3. יפתח עורך Apps Script עם פרויקט מקושר לגיליון.

## שלב 2: הפעלת V8 Runtime

חשוב מאוד — הקוד משתמש בתחביר מודרני (`Array.some`, `Object.keys` וכו').
1. בעורך ה-Apps Script: **Project Settings** (⚙️ בתפריט הצד).
2. ודא ש-**"Enable Chrome V8 runtime"** מסומן (ברוב הפרויקטים החדשים זה כבר ברירת מחדל).

## שלב 3: העלאת הקבצים

בעורך ה-Apps Script, מחק את `Code.gs` הריק שנוצר אוטומטית והוסף את כל הקבצים הבאים
(**File → New → Script file** עבור קבצי `.gs`, **File → New → HTML file** עבור קבצי `.html`):

**קבצי Script (.gs):**
Code, Utils, Database, CacheHelper, AuthService, CompetitionService, RotationService, ReviewService,
ManagerTaskService, BadgeService, WeekService, DashboardService, ActivityService,
AdminService, SetupService

**קבצי HTML:**
Index, Styles, Components, Scripts

**Landing.html** נשאר בחוץ בכוונה — הוא דף שיווקי סטטי, לא חלק מה-Web App עצמו. ראה שלב 3.5 למטה להסבר איך לחבר אותו.

העתק את התוכן המדויק מכל קובץ שקיבלת לתוך הקובץ המתאים בעורך (שים לב: כאשר יוצרים קובץ HTML חדש בשם "Index" וכו', Apps Script מוסיף את הסיומת `.html` אוטומטית — אל תוסיף אותה בשם).

## שלב 3.5: איפה שם Landing.html

`Landing.html` הוא קובץ HTML **עצמאי לגמרי** (לא Apps Script) — דף נחיתה שיווקי לפני הכניסה למערכת. הוא לא מועלה לעורך ה-Apps Script יחד עם שאר הקבצים. שתי אפשרויות:

1. **הכי פשוט:** ארח אותו בכל מקום שמארח קבצי HTML סטטיים (אתר החברה, Google Sites, GitHub Pages, או אפילו כקובץ מצורף במייל/וואטסאפ שנפתח ישירות בדפדפן).
2. אחרי שתפרוס את ה-Web App (שלב 5) ותקבל את ה-URL שלו, פתח את `Landing.html` ותחליף את כל 3 המופעים של `href="#"` בכפתורי "כניסה / הרשמה" ב-`href="[Web App URL שלך]"` בפועל — כך שהכפתורים יובילו למערכת האמיתית.

## שלב 4: הרצת ה-Setup Wizard

1. בעורך ה-Apps Script, בתפריט הפונקציות העליון בחר בפונקציה **`setupCompetitionSystem`**.
2. לחץ **Run** (▶️).
3. בפעם הראשונה תתבקש לאשר הרשאות — Google תציג אזהרה "האפליקציה לא מאומתת" (זה תקין לפרויקט אישי): לחץ **Advanced → Go to [project name] (unsafe) → Allow**.
4. ההרצה תיצור את כל 17 ה-Tabs, תמלא CONFIG, ותייצר ארגון Demo מלא: **2 מוקדים (שימור/שירות), 5 צוותים** (פרואקטיב/פולס/אירון בשימור, ליגה/טוי בשירות), ראשי צוות ואחמ"שים עם קודי PIN אישיים, נציגי Demo, ~38 בדיקות שיחה, משימות ו-Badges.
5. חזור לגיליון עצמו — תראה את כל ה-Tabs מלאים בנתונים.

> **טיפ:** לאחר ריצה ראשונה, ריענון הגיליון (F5) יוסיף גם תפריט **⚔️ Retention Battle** עם קיצור דרך להתקנה חוזרת ולסגירת שבוע — ישירות מהגיליון.

## שלב 5: פריסה כ-Web App

1. בעורך ה-Apps Script: **Deploy → New deployment**.
2. לחץ על סמל ה-⚙️ ליד "Select type" ובחר **Web app**.
3. הגדרות:
   - **Execute as:** Me (החשבון שלך)
   - **Who has access:** בחר לפי הצורך — "Anyone within [הארגון שלך]" מומלץ למוקד פנים-ארגוני, או "Anyone" אם צריך גישה כללית.
4. לחץ **Deploy**.
5. תקבל **Web app URL** — זה הקישור שכל אחמ"ש/ראש צוות/נציג ישתמשו בו כדי להיכנס למערכת.

## שלב 6: TV Mode

כדי להציג את המערכת על מסך במוקד ללא Sidebar ובקרות ניהול, השתמש בקישור עם פרמטר נוסף:

```
[Web App URL]?mode=tv
```

## שלב 7: החלפת נתוני ה-Demo בנתונים אמיתיים

מבנה ה-Demo כולל 2 מוקדים: **שימור** (צוותים: פרואקטיב, פולס, אירון) ו-**שירות** (צוותים: ליגה, טוי). אפשר לשנות שמות מוקדים/צוותים בחופשיות — מסך הכניסה נבנה על הצוותים בפועל בגיליון, לא על רשימה קבועה בקוד.

1. עבור למסך **ניהול** באפליקציה עצמה (לא בגיליון!) → הוסף/ערוך צוותים, ראשי צוות ונציגים. שים לב שכל צוות חדש דורש גם ערך `Division` (שימור/שירות, או מוקד נוסף לבחירתך).
2. לחלופין, ניתן לערוך ישירות את ה-Tabs `TEAMS`, `AGENTS`, `TEAM_LEADERS`, `USERS` בגיליון עצמו — כל עוד שומרים על מבנה העמודות. זה בטוח לעשות לפני שהתחילו להזין בדיקות שיחה אמיתיות.
3. מחק את שורות ה-Demo מ-`CALL_REVIEWS` אם אינך רוצה שישפיעו על השבוע הראשון האמיתי (שים לב: לאחר שמתחילים לעבוד עם נתונים אמיתיים, אין למחוק רשומות עוד — רק לבטל דרך `cancelCallReview`).
4. **נציג חדש שמתווסף** (דרך מסך ניהול או ישירות בגיליון) לא צריך PIN מראש — הוא יירשם בעצמו בכניסה הראשונה שלו דרך מסך "כניסה ראשונה", כמתואר בשלב 8 למטה.

## שלב 8: כניסה למערכת ואבטחה

**איך נציגים נכנסים בפעם הראשונה (ללא צורך בקוד מה-Admin):**
1. בפעם הראשונה, נציג לוחץ **"כניסה ראשונה — הרשמה כנציג"**.
2. בוחר מוקד (שימור / שירות) → צוות → את שמו מתוך רשימת הנציגים שעדיין לא נרשמו בצוות.
3. המערכת מייצרת קוד PIN אקראי בן 4 ספרות ומציגה אותו **פעם אחת בלבד** — הנציג חייב לשמור אותו, כי רק ה-Hash שלו נשמר בגיליון ואי אפשר לשחזר אותו.
4. מרגע זה, השם של הנציג נעלם ממסך "כניסה ראשונה" (כי הוא כבר "נתבע") ועובר להופיע רק במסך "כניסה" הרגיל, שם הוא יזין את אותו PIN בכל כניסה הבאה.

זה אומר שאין צורך שהאדמין ידפיס/ישלח PIN ידנית לכל נציג חדש — מספיק להוסיף את השם שלו למסך **ניהול → נציגים** (או ישירות ל-Tab `AGENTS` בגיליון), והוא ירשם בעצמו.

**ראשי צוות / אחמ"ש** לא עוברים את תהליך ההרשמה העצמית בכלל — זו הפרדה מכוונת בקוד עצמו (`registerAgent` בודק שהתפקיד הוא `AGENT` ודוחה כל ניסיון אחר). הם נכנסים אך ורק דרך הקישור **"כניסת ניהול / ראש צוות"** שבתחתית מסך הכניסה, עם קוד PIN אישי שכבר הוגדר לכל אחד מהם מראש ב-Setup (לפי הרשימה שסיפקת: ראשי צוות ואחמ"שים, כל אחד עם קוד משלו). מומלץ עדיין שכל אחד מהם ישנה את הקוד שלו בכניסה הראשונה (לחיצה על השם למעלה → "שינוי קוד PIN אישי"), במיוחד אם הקוד הופץ בכתב (וואטסאפ/מייל) בדרך אליהם.

**איך זה עובד מאחורי הקלעים:** כל התחברות (נציג או ניהול) בודקת PIN בצד השרת מול Hash שמור (לא בטקסט גלוי), ומנפיקה **Token חתום** (HMAC, עם Secret שנוצר אוטומטית ונשמר רק ב-Script Properties) בתוקף ל-12 שעות. כל פעולה רגישה במערכת מאמתת את הטוקן הזה בצד השרת בכל קריאה — כך שאי אפשר "להתחזות" למישהו אחר בעזרת ה-`userId` שלו בלבד.

**לחיבור Google Workspace אמיתי בעתיד** (שכבת אבטחה נוספת, לא חובה):
1. פרוס את ה-Web App עם **Who has access: Anyone within [הארגון שלך]** (דורש Google Workspace).
2. ב-`AuthService.gs`, בפונקציה `login`, ניתן להוסיף בדיקה נוספת: `Session.getActiveUser().getEmail()` חייב להתאים לעמודת `Email` של אותו `userId` בטבלת `USERS`, לפני שממשיכים לבדיקת ה-PIN.

## תחזוקה שוטפת

- **ניקוד שיחות (השימוש היומיומי העיקרי):** אחמ"ש נכנס ← מסך "ניהול" ← טאב **"🏆 ניהול תחרות"** (ברירת מחדל לאחמ"ש) ← בוחר מוקד → צוות → נציג (או לוחץ "🎲 שיחה רנדומלית" לדגימה אקראית) ← "➕ הוסף שיחה מתועדת" ← ממלא מספר לקוח ומדרג לפי 3 הקריטריונים + שימור מוצלח. הנציג רואה את השיחה שלו (בלי הערת האחמ"ש) תחת "📞 השיחות שלי".
- **סגירת שבוע**: כל יום ראשון (או בסוף מחזור התחרות), אחמ"ש/Admin לוחץ "🏁 סגור שבוע" במסך הניהול, או משתמש בתפריט ⚔️ בגיליון. שים לב: יש צוות מנצח **נפרד לכל מוקד** (שימור ושירות לא מתחרים זה בזה).
- **שינוי משקלי ניקוד**: מסך ניהול → הגדרות — משנה מיידית את כל החישובים לשבוע הנוכחי (וגם רטרואקטיבית להיסטוריה, מכיוון שהחישוב תמיד Live).
- **גיבוי**: מכיוון שהכל ב-Google Sheets, גיבויים אוטומטיים דרך Google Drive Version History תקפים כרגיל.
